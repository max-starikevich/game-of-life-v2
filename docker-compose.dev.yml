version: '3'

services:
  backend:
    build: ./docker/node
    volumes:
    - ./src/server:/home/node/app
    ports:
    - '9229:9229'
    - '3000:3000'
    networks:
    - dev-network
  webpack-dev-server:
    build: ./docker/node
    volumes:
    - ./src/client:/home/node/app
    ports:
    - '8080:8080'
    - '8888:8888'
    networks:
    - dev-network
networks:
  dev-network:
    driver: bridge
